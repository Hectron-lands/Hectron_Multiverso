apiVersion: v1
kind: Service
metadata:
  name: global-load-balancer
  namespace: hectron
  annotations:
    cloud.google.com/load-balancer-type: "External"
spec:
  selector:
    app: api-gateway
  ports:
  - port: 80
    targetPort: 3000
  type: LoadBalancer